export enum ButtonTypes {
  PRIMARY = 'primary',
  DEFAULT = 'default',
  DASHED = 'dashed',
  DANGER = 'danger',
  LINK = 'link',
}

export enum ButtonHtmlTypes {
  BUTTON = 'button',
  SUBMIT = 'submit',
}
